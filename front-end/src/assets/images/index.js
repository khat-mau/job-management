const images = {
    certificate1: require('./certification1.svg').default,
    certificate2: require('./certification2.svg').default,
    homeImage: require('./homeImage.svg').default,
    company: require('./company.svg').default,
    company2: require('./company2.svg').default,
};

export default images;
